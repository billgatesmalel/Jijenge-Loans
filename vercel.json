{
  "services": {
    "frontend": {
      "root": "frontend",
      "framework": "vite"
    },
    "jijenge-loans-backend": {
      "root": "backend",
      "entrypoint": "src/main.ts",
      "routePrefix": "/",
      "buildCommand": "npm install --include=dev && npm run prisma:generate && npm run build"
    }
  }
}
