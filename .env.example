# ============================================================
# Jijenge Loans - Production Environment Template
# Copy to .env for local development and setup variables
# ============================================================

# ── Supabase PostgreSQL Database (Prisma ORM) ───────────────
# Required: Supabase Transaction Pooler URL (Port 6543)
DATABASE_URL="postgresql://postgres.xxx:password@aws-0-eu-central-1.pooler.supabase.com:6543/postgres?pgbouncer=true"

# Required: Supabase Direct Connection URL (Port 5432) for Prisma Migrations
DIRECT_URL="postgresql://postgres.xxx:password@aws-0-eu-central-1.pooler.supabase.com:5432/postgres"

# ── JWT Authentication ──────────────────────────────────────
# Required: Secret key for signing access & refresh tokens (Min 32 chars)
JWT_SECRET="jijenge_loans_super_secure_jwt_secret_min32chars_production"

# ── Super Admin Initial Seed Credentials ────────────────────
SUPER_ADMIN_USER="admin@jijengeloans.co.ke"
SUPER_ADMIN_PASS="Jijenge2026!SecureAdminPass"

# ── PalPluss M-Pesa Payment Gateway ─────────────────────────
# Required: PalPluss Live API key
PALPLUSS_API_KEY="pk_live_jijenge_palpluss_api_key_here"

# Required: Callback Base URL (Vercel or Render backend host)
PALPLUSS_CALLBACK_BASE_URL="https://jijengeloans.co.ke"

# Required: Webhook signature verification secret
PALPLUSS_WEBHOOK_SECRET="generate_random_secret_string_here"

# ── Capcom6 Android SMS Gateway (sms-gate.app) ───────────────
SMSGATEWAY_USERNAME="your_sms_gateway_user"
SMSGATEWAY_PASSWORD="your_sms_gateway_password"
SMSGATEWAY_BASE_URL="https://api.sms-gate.app/3rdparty/v1"
SMSGATEWAY_SIM_NUMBER=1

# ── Node Server Config ──────────────────────────────────────
PORT=5000
NODE_ENV=development
ALLOWED_ORIGIN="http://localhost:3000"
